# Vault CE default

This repository now defaults to Vault Community Edition.

You do not need any trial license to run the demo.

Use one of these entry points instead:

```bash
make setup
```

or:

```bash
./quick-start.sh
```
